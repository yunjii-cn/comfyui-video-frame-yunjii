feat: 输出可见性修复——Stitcher官方ui进「已生成」+ MTB侧栏视频显示

一、ComfyUI 官方输出标准（用户问询的结论）：
   OUTPUT_NODE=True 且 FUNCTION 返回 {"ui": {...}, "result": (...)}
   (execution.get_output_from_returns 只认 dict 的 'ui' 键)。
   SaveImage→images；SaveVideo→videos；VHS_VideoCombine→gifs。
   旧前端画廊只消费 images(需首帧PNG封面)，节点视频播放器读 gifs/videos。

二、Stitcher(拼接节点)修复「已生成」不显示：
   - 原先返回纯 tuple 无 ui → history outputs 为空 → 画廊无内容；
   - 全部 return 点改 {"ui": _build_output_ui(...), "result": (...)}；
   - 成功路径(单段直出/多段拼接)加 register_video_to_history 双保险
     (Runner 内联执行会破坏外层 history，与 Composer 同源通道)。

三、MTB(comfy_mtb) 输入输出侧栏「只显示图片」修复(_patch_mtb.py 重放)：
   - endpoint.getUserImages 白名单加 mp4/webm/mov/mkv；
   - 视频条目 URL 走官方 /view(FileResponse 支持 Range，<video>可播；
     /mtb/view 用 PIL 解码视频必失败)；
   - getUserImageFolders output 追加二级子目录("一级/二级"，mtime降序
     限量40)——成片在 output/yunjii_v2v/<run_id>/ 二级，原先进不去；
   - sidebar.js 按扩展名动态渲染 <video>(autoplay/muted/loop)。
   实测：yunjii_v2v/<run> 列出成片mp4+封面，URL 带子目录走 /view。

生效需重启 ComfyUI(endpoint.py)并强刷浏览器(Ctrl+F5, js)。
