# P3 落地计划 · Phase 2 效果模块（运镜/创意/增强）

> 生成日期：2026-07-26 · 基于代码真实状态（S1/S2 已修、SCAIL-2 后端已接、死代码已清、`effects/` 仅剩 base.py）
> 原则：**服务"长视频完美模仿"主目标**，先做直接提升模仿质量的，再做锦上添花的。

---

## 〇、排序逻辑（为什么是这个顺序）

PLAN.md 的 Phase 2 写的是"运镜→创意→增强"，但那是产品视角。工程视角下：

1. **模仿质量**是当前唯一被用户明确要求的目标（"长视频的完美模仿"）；
2. SCAIL-2 真模型验证会揭示字段名差异 → **planner 后端感知**是它的前置依赖；
3. 增强模块（超分/插帧）是**纯后处理**，与生成链解耦、风险最低、见效最快；
4. 运镜/创意模块依赖姿态变换管线，是最重的，放最后。

因此实际落地顺序：**P3-A 后端感知分段 → P3-B 效果管线骨架 → P3-C 增强模块 → P3-D 运镜模块 → P3-E 创意模块**。

---

## P3-A：planner 后端感知分段（模仿质量的地基）⭐ 最优先

**问题**：现在 planner 只有骨骼路线的 `4k+1` 帧规则；SCAIL-2 路线需要"每段 81 帧、段间重叠 5、有效步进 76"。不改这个，SCAIL 路线的段边界就会跳帧/重影。

**改动**：
- `types.py`：`SegmentPlan` 增加 `backend` 字段（`"wanvideo" | "scail2"`）。
- `planner.py`：新增"后端"输入参数；SCAIL 模式下按 `步进76/段长81/重叠5` 切段（镜头边界仍优先），WanVideo 模式保持 4k+1 不变。
- `runner.py`：把「生成后端」选择传给 plan 校验，后端不匹配时给出明确警告而非静默错跑。

**验收**：桩验证脚本扩展——对 300 帧输入，SCAIL 模式产出段 `[0,80] [76,156] [152,232] [228,299]`，重叠恰好 5 帧。

**依赖**：无（可立即做）。但字段名回填（`pose_video`/`reference_image`/`previous_frame_count`）需等真模型跑通。

## P3-B：效果管线骨架（EffectPipeline）

**目标**：落地 PLAN 里的 Stage 1→2→3→4→5 叠加管线，但**只建骨架 + 一个真实现**，不空实现（上次 `mimic.py` 空实现被当死代码删了，教训：没有消费者的接口不要写）。

**改动**：
- `engine/effects/base.py` 已有 `EffectModule/EffectContext` 协议 → 补 `engine/pipeline.py`：按 stage 排序依次调用 `transform_poses/transform_prompts/transform_params/transform_stitch`。
- 第一个真模块：`effects/mimic.py` 重建为**真实现**——包装现有"参考图策略 + previous_frames 串联"逻辑（从 runner 里抽出来），让模仿成为第一个走管线的模块。
- runner 增加可选「效果模块」输入（JSON 列表），为空时行为与现在完全一致（零回归）。

**验收**：不选效果模块时输出与现状 bit 级一致；选 mimic 时行为等价。

## P3-C：增强模块（Enhance）——最快见效

**目标**：拼接后的成片后处理，与生成链解耦。

**优先子项**（按实用性）：
1. **帧率提升**：RIFE 插帧（ComfyUI 已有 `ComfyUI-Frame-Interpolation` 节点，adapter 里追加子图即可）——SCAIL-2 输出 16fps，插到 32/60fps 对"完美模仿"观感提升最大；
2. **画质提升**：接 Upscale 模型节点（如 2xLexicaRRDBNet）做 480p→960p；
3. 去抖动/背景替换：**暂缓**（SCAIL-2 生成的视频本身无手持抖动，背景由参考图/提示词控制，需求弱）。

**实现方式**：`effects/enhance.py` 的 `transform_stitch` 在 stitcher 输出后追加 ffmpeg/ComfyUI 子图步骤。

## P3-D：运镜模块（Cinematic）

- 把 PLAN 里的 `CAMERA_MOTION_PRESETS`（9 种）实现为 `transform_prompts`（注入运镜后缀）+ `transform_params`；
- `TRANSITION_PRESETS`（10 种）实现为 `transform_stitch`（stitcher 已有交叉淡化，扩展为转场模板库）；
- 一镜到底 = 现有 prev_last_frame 策略 + 大重叠，基本是配置组合，成本低。

## P3-E：创意模块（Creative）

- **节奏重构**（节拍检测 + 时间重映射）最有产品差异性，但需要音频分析依赖（librosa）——单独评估包体积后再定；
- 动态增强/混合融合/超现实：依赖姿态序列变换，仅对骨骼路线有意义（SCAIL-2 无骨架），优先级最低。

---

## 里程碑与顺序总览

| 阶段 | 内容 | 前置 | 风险 |
|------|------|------|------|
| **M0**（外部） | SCAIL-2 真模型验证 + 字段回填 | 下载权重（见验证清单，3090 用 fp8_scaled） | 低（纯字段对齐） |
| **M1** | P3-A 后端感知分段 | 无 | 低 |
| **M2** | P3-B 效果管线骨架 + mimic 真模块 | M1 | 中（runner 重构，需零回归验证） |
| **M3** | P3-C 增强（插帧→超分） | M2 | 低 |
| **M4** | P3-D 运镜（预设+转场） | M2 | 中 |
| **M5** | P3-E 创意（节奏重构优先） | M4 | 高（新依赖） |

**建议节拍**：M0 由你本机下载权重启动（可与 M1 并行）；M1 现在就能做；M2 之后每个里程碑单独提交 + 推送。

---

## 进度同步（按推荐顺序推进）

- **M1 / P3-A**：已完成。types.backend 字段+常量 → planner 后端感知分段(81/5/76) → runner 后端一致性守卫（编译+桩验证通过）。
- **M2 / P3-B**：✅ 已完成并通过验证（`.workbuddy/verify_p3b.py` 全 18 项 PASS）。交付：
  - `engine/pipeline.py`：`EffectPipeline`（按 stage 排序、逐模块 try/except 容错）+ `build_pipeline(modules_json)` 注册表（已知模块 `mimic`，预留 enhance/cinematic/creative）。
  - `engine/effects/mimic.py`：重建为**真实模块**（上轮 P1 删的空壳不再重建）。生成侧对非首段 prompt 追加身份一致性后缀；拼接侧对 auto/cross_dissolve 把淡化帧抬高到 12；参数侧默认透传（保证"选 mimic 时行为与现状等价"）。
  - `engine/effects/__init__.py`：重新导出 `MimicModule`。
  - `engine/runner.py`：加可选「效果模块」输入，生成前对每段 prompt/params 应用管线；空=透传（零回归）。
  - `engine/stitcher.py`：加可选「效果模块」输入，拼接前对 (mode, fade_frames) 应用 `transform_stitch`；空=透传（零回归）。
  - **零回归关键**：不选效果模块时，runner/stitcher 行为与现状完全一致（验证脚本已证明空管线所有 transform 透传）。
  - **下一步（待你确认）**：P3-C 增强模块（RIFE 插帧→超分，接 ComfyUI 子图/ffmpeg，与生成链解耦、见效最快）。
- **M3 / P3-C**：✅ 已完成并通过验证（`.workbuddy/verify_p3c.py` 全 17 项 PASS）。交付：
  - `engine/effects/base.py`：`EffectModule` 协议新增 `transform_output(video_path, context)` 默认透传——增强模块挂载"拼接后最终成片"的钩子。
  - `engine/pipeline.py`：`EffectPipeline` 新增 `transform_output`（逐模块容错）；`build_pipeline` 升级支持 `{"name","params"}` 带参写法；`KNOWN_MODULES` 登记 `enhance`。
  - `engine/effects/enhance.py`：**真实增强模块** `EnhanceModule(stage=20)`。经 ffmpeg 做 2x 超分(scale lanczos) + 帧率提升(minterpolate 运动补偿插帧，SCAIL-2/WanVideo 16fps→32fps)。ffmpeg 缺失/失败→返回原路径并告警（不崩、不丢片）。命令构造为纯函数（便于无 ffmpeg 环境单测）；中间文件自动清理（绝不删原始输入）。
  - `engine/effects/__init__.py`：导出 `EnhanceModule`。
  - `engine/stitcher.py`：音频合成后对最终成片调用 `pipeline.transform_output`（复用已构建的 effect_pipeline）；空管线透传。
  - **关于 RIFE**：RIFE 是 ComfyUI 节点非 CLI 工具，无法在"拼接后文件"这一步直接调用，故先用 minterpolate 作依赖-free 兜底；结构上已预留后续替换为 RIFE 子图任务的位置。
  - **零回归**：不选效果模块时整条链路透传；选 enhance 时若环境无 ffmpeg/文件异常也只跳过增强、保留成片。
  - **下一步（待你确认）**：P3-D 运镜模块（Cinematic 预设+转场）；P3-E 创意模块（节奏重构，需 librosa 新依赖）。
- **M4 / P3-D**：✅ 已完成并通过验证（`.workbuddy/verify_p3d.py` 全 30 项 PASS）。交付：
  - `engine/effects/cinematic.py`：**真实运镜/电影感模块** `CinematicModule(stage=15)`。
    - **运镜预设** `CAMERA_MOTION_PRESETS`(9 种：电影感/推拉/环绕/手持/固定/升降/横移/俯仰/跟随) → `transform_prompts` 注入相机运动英文后缀；带「已含则不重复追加」幂等保护，可与 mimic 身份后缀叠加而不冲突。
    - **转场预设** `TRANSITION_PRESETS`(10 种) → `transform_stitch` 映射为 (mode, fade_frames)，把 stitcher 的交叉淡化扩展为转场模板库（硬切/叠化/电影叠化/快叠/长叠/柔光/戏剧/瞬切/黑场/轻叠）。
    - **电影调色** `CINEMATIC_GRADES`(电影感/清新/复古/黑白/高对比 + none) → `transform_output` 经 ffmpeg 标准滤镜链(eq/colorbalance/vignette/hue) 做色彩分级；ffmpeg 缺失/执行失败→返回原片（不崩、不丢片）。
    - `transform_params` 默认透传；`stabilize=True` 时仅温和抬 cfg≥6.0 / 降 denoise≤0.7，不覆盖用户显式设定。
  - `engine/pipeline.py`：`KNOWN_MODULES` 登记 `cinematic`。
  - `engine/effects/__init__.py`：导出 `CinematicModule`。
  - **与 mimic 的相位关系**：mimic(stage=10) 先跑（身份后缀 + 抬高淡化帧），cinematic(stage=15) 后跑（叠加运镜 + 据自身转场二次设淡化帧）；两段各司其职、互不覆盖。
  - **零回归**：不选效果模块时整条链路透传；选 cinematic 时默认仅运镜+转场（grade=none 不调色），需显式开启调色。
  - **下一步（待你确认）**：P3-E 创意模块（节奏重构优先，需 librosa 新依赖，单独评估包体积）。
- **M5 / P3-E**：✅ 已完成并通过验证（`.workbuddy/verify_p3e.py` 全 20 项 PASS）。交付：
  - `engine/effects/creative.py`：**真实创意模块** `CreativeModule(stage=30)`。
    - **节奏重构（音频驱动）**：`audio_path` + 环境装 librosa 时，`_analyze_audio` 惰性 import librosa、读音频 RMS 能量包络、按段数均分、给每段 prompt 注入「该段运动强度」描述（calm/steady/dynamic/explosive 四档），使生成画面运动节奏跟随音乐；纯 Python 容错（env.flatten→list，避免引擎对 numpy 硬依赖）；无 librosa/无音频/分析失败→返回 None、prompt 原样透传（零回归、不崩）。
    - **创意节奏预设** `PACING_PRESETS`(顺滑/动感/蒙太奇/电影感/呼吸)→`transform_stitch` 映射 (mode, fade_frames)，在 cinematic(stage=15) 之后二次裁决最终转场。
  - `engine/pipeline.py`：`KNOWN_MODULES` 登记 `creative`；`build_pipeline` 文档示例补 creative 带参写法。
  - `engine/effects/__init__.py`：导出 `CreativeModule`。
  - **依赖评估（回应计划中「librosa 包体积单独评估」）**：librosa 本身几 MB，但会拉 numpy/scipy/numba/soundfile/resampy 等；在已有 torch/numpy 的 ComfyUI 环境里边际增量约 **100–200MB**。是否安装由用户决定——**不装也能用 PACING 预设**（仅跳过节奏重构）。
  - **架构边界（诚实标注）**：真正「按节拍时间重映射（重切段）」需重跑 planner 用节拍感知分段边界，属后续增强（M6+）；本模块在当前架构内做的是「运动强度/节奏预设」层面的节奏重构，已是最优可落地形态。
  - **零回归**：不选效果模块时整条链路透传；creative 默认仅应用 PACING 预设（motion_prompt 默认 True 但需 audio_path+librosa 才激活）。
- **P3 全部完成（A→B→C→D→E）**。所有效果模块均经桩验证脚本证明零回归 + 真实生效。M0（SCAIL-2 真模型验证）状态见下方 M0 行 —— **已端到端跑通并修复稳定性**。
- **M6 / 贯穿节点（Composer）**：✅ 已完成并通过验证（`.workbuddy/verify_composer.py` 全 13 项 PASS）。这是对 P3 集成验证（M5 后自检）所发现「runner 与 stitcher 各自吃一份 `效果模块`、填不一致会割裂效果」隐患的根治：
  - **新增 `engine/composer.py`**：`YunjiiVideoImitator`（显示名「完美模仿一键 ⚡ (Yunjii V2V)」）。把「分段规划 → 链式执行(生成) → 无缝拼接」三节点合为**单一节点**，合并两节点全部入参。
  - **核心价值——单一 `效果模块` 同时份传给生成相位(runner)与拼接相位(stitcher)**：用户只填一次，两侧表现必然一致；根治割裂。空=透传，行为与现状完全一致。
  - **编排逻辑**：仅规划模式短路（不进生成/拼接，直接返回计划摘要）；执行/续跑先跑 runner，成功后再跑 stitcher；任一阶段失败即提前返回、不掩盖错误；返回 `(最终视频路径, 执行日志+拼接报告, 完成状态)`。
  - **零回归**：不改动现有 runner/stitcher 任何逻辑，composer 仅做编排；两个子节点仍独立可用（老工作流不受影响）。
  - **注册**：根 `__init__.py` 的 `NODE_CLASS_MAPPINGS` / `NODE_DISPLAY_NAME_MAPPINGS` 已登记（`YunjiiVideoImitator`）。
- **M0（2026-07-30 已完成）**：SCAIL-2 真模型端到端跑通 + **真骨架(NLF)路线稳定性修复**。实际落地路径：适配器 `engine/adapters/scail.py` 已重写为驱动官方 embeds 子流程（`WanVideoAddSCAILReferenceEmbeds`+`WanVideoAddSCAILPoseEmbeds`+`WanVideoEmptyEmbeds`+`WanVideoSamplerv2`），工作流 `workflows/SCAIL2_embed子流程_官方_20260728_0230.json` 内嵌 NLF 真骨架分支（`NLFModelLoader→NLFPredictPoses→RenderNLFPoses→WanVideoAddSCAILPoseEmbeds`），权重(14B fp8_scaled + NLF fp16 + detection onnx + sam3 + clip_vision + umt5) 已就位，`_official_pose_runnable()` 返回 True 自动走真骨架、不再回退驱动帧。
  - **关键稳定性修复（2026-07-30）**：RTX 3090(sm_86) 上 `WanVideoTorchCompileSettings` 接 `WanVideoModelLoader.compile_args` 会让 fp8_e4m3fn 权重被 `torch.compile`，采样首步触发 Windows 堆损坏 `0xc0000374` 间歇性硬崩、杀掉整个 ComfyUI 进程。修复：① 工作流层断开 compile 连线并移除编译节点；② 代码层在 `ComfyUI-WanVideoWrapper/nodes_model_loading.py` 的 fp8+cc<8.9 分支由「仅警告」改为**强制 `compile_args=None`**（双保险，任何工作流都不会再触发）。
  - **稳定性验证**：连跑 3 轮（每轮干净重启进程、真实重跑排除缓存），全部 PASS、服务端累计 `0xc0000374` 崩溃 = 0，产出 `WanVideo_SCAIL_00009/00010/00011.mp4`。② 真骨架链路现已生产可用。
  - **③ SAM3 v2 完整骨架（进阶）**：仍**暂缓**。它在 ② 基础上仅多 SAM3 语义部位掩码，且需升级 WanVideoWrapper 核心依赖/重架构工作流（消费节点 `WanVideoAddSCAIL2ConditionEmbeds` 本机未注册），边际收益小、风险高，已在 7-28 评估为「不值得现在做」。
