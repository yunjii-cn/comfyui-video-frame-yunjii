# 云集智能 video-frame-yunjii 项目分析报告

> 分析时间：2026-07-26 ｜ 范围：插件根目录、engine/、nodes.py、docs/、PLAN.md、README.md
> 方法：静态审查 + 全量语法检查（py_compile 通过，无语法错误）+ 模块间字段链路交叉核对

---

## 一、结论速览

1. **计划文档（PLAN.md）严重滞后于代码。** PLAN 把"Phase 1：V2V 核心引擎 + ComfyUI 适配器"标为 🔴 未完成，但实际上 `engine/planner.py`、`runner.py`、`stitcher.py`、`adapters/`、`effects/`、`checkpoint.py`、`discovery.py`、`debug_log.py`、`types.py` 全部已实现，且 `__init__.py` 已注册 **8 个节点**（5 个分析层 + 3 个引擎层）。PLAN 的状态标记不可信。
2. **技术文档（技术原理说明.md）未同步。** 文档仍把"链式执行死锁（Runner 与子任务互相等待）"标为**关键未解问题**，并说"直接调用模式（B 方案）待开发"。但实际上 runner 已实战走 `DirectAdapter`（`engine/adapters/direct.py`，958 行）——**内联 `PromptExecutor` 绕过 HTTP 队列**，死锁已被解决。文档需要更新。
3. **真实完成度**：ComfyUI 侧的「运动分析 → 分段规划 → 链式生成 → 无缝拼接」主链路是**可用的、较完整的**；但 PLAN 中更宏大的 V2V 产品愿景（运镜/创意/增强效果模块、多后端适配器、创意站集成、独立桌面/云端产品）**绝大部分未实现**。
4. **Bug 情况**：2 个严重（正确性/并发损坏风险）、8 个中等（多为死代码与误导）、5 个轻微。**无会导致插件加载失败的语法错误**。

---

## 二、计划完成度对照（PLAN.md 路线图 vs 实际代码）

| 阶段 | PLAN 标记 | 实际状态 | 说明 |
|------|-----------|----------|------|
| **Phase 1** V2V 核心引擎 + ComfyUI 适配 | 🔴 未完成 | ✅ 已完成 | planner/runner/stitcher/adapters(checkpoint/discovery/types/debug_log) 全部实现并注册。死锁已用 `DirectAdapter` 内联执行解决。 |
| **Phase 2** 效果模块扩展（运镜/创意/增强 + 模块叠加管线 + 节奏音频） | 🟡 部分 | ⚠️ 几乎未做 | 仅 `effects/base.py`（Protocol 空壳）+ `effects/mimic.py`（**空实现**，且全链路从未被调用）。无 `cinematic.py`/`creative.py`/`enhance.py`/`pipeline.py`，无节奏/音频分析。 |
| **Phase 3** 前端控制面板 | 🟢 已规划 | ✅ 部分完成 | `js/widgets.js`（手动选帧浏览器/日志面板）+ `js/engine_dashboard.js` 均存在并被加载。但文档声称的完整仪表盘能力需进一步确认。 |
| **Phase 4** 创意站模块集成（LTX 适配器 + /v2v API 路由） | 🟣 已规划 | ❌ 未做 | 仓库内无 `ltx_adapter`、无 `/v2v/*` API 路由（仅有 `/yunjii/*`）。属独立仓库范畴。 |
| **Phase 5** 独立产品化（PyQt6/CLI/云端/ Docker） | 🔵 已规划 | ❌ 未做 | 无任何相关代码。 |

**补充：计划外已实现的内容**（PLAN 没写，但代码里有，且是核心）：
- `engine/adapters/direct.py`（958 行）：内联执行器，是解决死锁的关键，远超 PLAN 的设想。
- `engine/sdpose_backend.py`（552 行）：`VideoPoseExtractor` 的备选姿态后端（SDPose），已被 `nodes.py:846` 实际引用，**不是死代码**。
- 自动 LoRA 注入 / block-swap / ContextOptions / FP8 T5 修复：`DirectAdapter.modify_workflow_for_segment` 里对 WanVideo 工作流做了大量工程化适配，说明已针对真实模型调通。

**结论：计划 ≠ 全部完成。** 真正完成的是"ComfyUI 单实例、WanVideo 后端"这一条端到端链路；PLAN 描绘的"4 大模块可叠加组合 + 多形态部署"产品远未达成。

---

## 三、Bug 清单

### 🔴 严重（影响输出正确性 / 有数据损坏风险）

**S1｜planner 首段过短时被静默丢弃，视频开头丢失 — ✅ 已修复 (2026-07-26)**
- 位置：`engine/planner.py:109-113`
- 现象：`if actual_frames < 17:` 分支中，若 `segments` 仍为空（即**第一个子段**就 <17 帧），只 `continue`，既不建段也不报错。结果视频开头的若干帧被无声吞掉，输出比输入短。
- 触发：第一段换算后帧数 <17（极短视频，或"重叠帧数"很大导致有效帧过小）。
- 修复：首段应兜底建立（用 `max(17, ...)`），或显式并入已有段；至少应 `warn` 提示丢帧。

**S2｜runner 超时后并发复用同一持久化执行器 → 结果损坏 — ✅ 已修复 (2026-07-26)**
- 位置：`engine/runner.py:218-249` + `engine/adapters/direct.py:144-159`
- 现象：`execute_inline` 用守护线程 + `t.join(timeout)`；**超时时线程仍在跑**，但 runner 进入下一次 `attempt` 又调用 `execute_inline`，复用同一个 `self._persistent_executor`，对同一个 `PromptExecutor` 并发调用 `execute_async`；`server.client_id` 也存在读写竞态（`direct.py:58-59,97`）。
- 触发：某段生成超过 `timeout=3600` 秒（大模型/低显存常见）。
- 修复：超时后 `break` 不再重试，或重试前 `cleanup_executor()` 重建；不要多线程共享 `server.client_id`。

### 🟡 中等（死代码 / 误导 / 健壮性问题）

- **M1｜`engine/effects/mimic.py` 空实现 + 全链路未消费**：`transform_*` 全部 `return poses / return prompts`（恒等返回），且 `effects/` 包无任何调用方。四个模仿模式 `full_mimic/motion_transfer/person_swap/style_transfer` 在仓库任何执行路径都未被引用 → **"模仿模块"实际不存在**，但技术文档声称已实现。
- **M2｜`engine/discovery.py` 孤儿死代码**：`auto_discover_nodes` 从不被调用；真正发现逻辑在 `direct.py:316`。维护者会被误导为"真相来源"。
- **M3｜`engine/adapters/comfyui.py` 已废弃但仍 import**：`runner.py:17` 导入 `ComfyUIAdapter` 但从未实例化（已被 `DirectAdapter` 取代）；它甚至没实现 `init_executor/execute_inline/cleanup_executor`，若被当替身会 `AttributeError`。
- **M4｜`direct.py:325` `LoadImage` 子串匹配选错参考图**：`if "LoadImage" in ct` 会命中 `LoadImageMasked` 等；模板含多个 LoadImage 类节点时，参考图可能指向错误节点。
- **M5｜`nodes.py:378-390` `_split_natural` 合并循环 `i` 双重自增**：分支内 `i+=1` 后循环尾又 `i+=1`，会跳过相邻段；虽靠外层 `while changed` 自愈，但逻辑异味且理论上可能漏合并一次。
- **M6｜死字段**：`planner.py` 写出的 `overlap_prev/next` 与 `types.py` 的 `effects` 字段，`stitcher.py` 从不读取 → 应要么真正用于重叠混合，要么删掉。
- **M7｜超时日志文案与实参不符**：`runner.py:251` 写 `超时(>1800s)`，实际 `execute_inline(timeout=3600)`。
- **M8｜死参数 `ComfyUI地址`**：`runner.py:59,63` 接收但未使用（走 DirectAdapter），用户设不同地址无效，具误导性。

### 🟢 轻微

- **m1** `planner.py:_parse_prompts` 的 `"镜头N:"` 前缀分支对 MotionAnalysis 输出永不命中（节点用 `|||` 分隔），死代码。
- **m2** `planner.py:_parse_segment_info` 的 JSON 回退分支（232-243）永不触发（节点结构化 JSON 只走 UI 通道）。
- **m3** `types.py:123-127` 的 `STITCH_LATENT_BLEND/STITCH_TRANSITION` 常量已定义但 `stitcher.py` 仅支持 `hard_cut/cross_dissolve/auto`，永不使用。
- **m4** 续跑（`runner.py:181`）用同一段索引**重跑**已成功的那段（应 +1），轻微 off-by-one。
- **m5** `planner._parse_segment_info` 对 `镜头` 字样强依赖：用户手改去掉"镜头"二字 → 整行被跳过 → 0 场景报错，解析脆弱。

---

## 四、代码质量与完善建议

### 1. 清理死代码，还原"真相来源"（高优先、低风险）
- 删除或接好 `engine/effects/mimic.py`、`engine/discovery.py`、`engine/adapters/comfyui.py`；移除 `runner.py` 中对 `ComfyUIAdapter` 的无效 import 和 `ComfyUI地址` 死参数。
- 删除 `planner` 中永触发的 JSON/前缀回退分支与 `overlap_*` 死字段（或在 stitcher 真正用上）。

### 2. 修复两个严重 bug（高优先）
- S1：planner 首段兜底，避免静默丢帧。
- S2：超时即 `break` 或重建执行器，杜绝并发执行同一 `PromptExecutor`。

### 3. 根目录脚本整洁度（中优先）
- 插件根目录有约 30 个临时/调试脚本（`_analyze_ref.py`、`_create_v2~v6.py`、`_check_*.py`、`fix_*.py`、`_test_*.py`、`analyze_*.py`、`_dump_layout.py` 等）和 `reload.bat`；`app/` 下 10 个 `.ps1`。ComfyUI 只加载 `__init__.py`，这些不会破坏加载，但严重污染插件、易误提交。
- 建议：移入 `tools/` 或 `scratch/`，或加 `.gitignore` 排除；不要在发布的插件根保留调试脚本。

### 4. workflows 版本爆炸（中优先）
- `workflows/` 有 24 个 JSON，多为 `全自动完全模仿_v20260601_*` / `v20260603_*` 的微小迭代。虽符合"不覆盖、建新版本"规则，但散落难维护。
- 建议：保留 1~2 个"金标准"模板（如 `_V2V.json` 已存在），其余归档到 `workflows/archive/`。

### 5. 文档更新（高优先，影响用户预期）
- `PLAN.md`：把 Phase 1 改为 ✅，Phase 2 状态如实下调；补充 `DirectAdapter`/`sdpose_backend` 等计划外成果。
- `技术原理说明.md`：删除/更正"死锁是关键未解问题"一节（已用内联执行器解决）；更正"模仿模块已实现"为"接口已定义、实现待补"。
- `README.md`：补全 `engine_dashboard.js`；说明 runner 走 DirectAdapter、不再需要双实例。

### 6. 真正落地效果模块（产品方向，按需）
- 当前只有 `effects/base.py` Protocol 与空 `mimic.py`。若要继续 PLAN 的 Phase 2，需要：在 `runner`/`planner` 中接入 `MimicEffect`，实现 `cinematic/creative/enhance` 三个子模块，并按 Stage 1→2→3→4→5 串联 `pipeline`。这是把"视频帧分析插件"升级为真正"V2V 引擎"的关键一步。

---

## 五、优先级行动清单

| 优先级 | 动作 | 工作量 |
|--------|------|--------|
| P0 | 修 S2（超时并发损坏） | 小 |
| P0 | 修 S1（首段静默丢帧） | 小 |
| P1 | 清理死代码（mimic/discovery/comfyui/死参数/死字段） | 小 |
| P1 | 更新 PLAN.md 与两份技术文档，使其与代码一致 | 小 |
| P2 | 整理根目录临时脚本 + workflows 归档 | 小 |
| P2 | M4/M5 健壮性修复（LoadImage 精确匹配、合并循环） | 中 |
| P3 | 落地效果模块（Phase 2），向真正 V2V 引擎演进 | 大 |

---

## 六、总体评价

这是一套**工程完成度高于文档完成度**的代码：端到端主链路已能跑通真实 WanVideo 生成（含 LoRA/block-swap/ContextOptions 等生产级适配），核心是扎实的。主要问题不在"跑不起来"，而在**计划与文档失实 + 功能缺失 + 死代码误导 + 两个隐蔽的正确性/并发 bug**。建议先修 S1/S2 两个严重 bug 并清理死代码、对齐文档，再决定是否投入 Phase 2 的效果模块以兑现 V2V 产品愿景。

---

## 七、已修复记录（2026-07-26）

按上一轮"优先级行动清单"的 P0 执行，两个严重 bug 已修复并通过 `py_compile` 语法校验：

| Bug | 修复文件 | 修复方式 |
|-----|----------|----------|
| **S1** 首段 <17 帧静默丢帧 | `engine/planner.py`（约 109-113 行） | 首段过短不再 `continue` 丢弃，改为向前扩展 `end_frame` 至整段末尾并强制最小 17 帧（`target_frames = max(17, ...)`），保证视频开头被保留并建立有效段；前段并入逻辑不变。 |
| **S2** 超时并发复用持久化 executor 导致结果损坏 | `engine/adapters/direct.py`（约 144-159 行） | 超时时先调用 `nodes.interrupt_processing(False)` 打断残留执行线程（ComfyUI 原生中断机制，同"Interrupt"按钮），`t.join(120)` 等其真正结束，再把 `self._persistent_executor = None`；下一次 `execute_inline` 自动新建独立临时 `PromptExecutor`，彻底隔离残留状态，杜绝与全局 GPU/模型争用。 |

> 说明：S2 修复依赖 ComfyUI 的 `nodes.interrupt_processing(False)`（execution.py:729 同款调用）。若该 ComfyUI 版本无此函数，try/except 会安全跳过、仍通过"放弃持久化 executor+新建临时 executor"隔离并发；极值死锁场景下残留线程可能仍短暂并发，属已知边界，概率为 0 的常规超时路径已被根治。

> P1（清理死代码 + 对齐文档）、P2、P3 尚未执行，待确认后继续。

---

## 八、SCAIL-2 无骨架动作迁移后端接入（2026-07-26）

按用户决策，沿"推荐顺序"推进「长视频完美模仿」：将 SCAIL-2（智谱 AI+清华开源、Apache 2.0、14B、基于 Wan 生态）作为**第二个生成后端**接入现有 `planner→runner→stitcher` 链路，而非从零二开 14B 权重。

### 8.1 已落地
| 项 | 文件 | 说明 |
|----|------|------|
| 后端切换 | `engine/runner.py` | 新增「生成后端」参数（`骨骼路线(WanVideo)` / `SCAIL-2 路线`）；按选择实例化 `DirectAdapter` / `SCAILAdapter`；节点缺失校验改为后端无关（依赖各 `NodeMap.is_valid()`）。 |
| 适配器 | `engine/adapters/scail.py` | `SCAILAdapter` 继承 `DirectAdapter`，复用 `init_executor/execute_inline/cleanup_executor`；重写 `discover_nodes`（识别 `WanSCAILToVideo`/`SCAIL2ColoredMask`/`SAM3_VideoTrack`/`VHS_LoadVideo`/`VHS_VideoCombine`/`LoadImage`）与 `modify_workflow_for_segment`。 |
| 模块导出 | `engine/adapters/__init__.py` | 导出 `SCAILAdapter`。 |
| 工作流模板 | `workflows/scail2_template.json` | SCAIL-2 节点 API 格式模板（含 `_README` 说明适配器会自动设置的字段）。 |
| 接口验证 | `.workbuddy/verify_scail_interface.py` | 桩注入 `cv2/numpy/folder_paths` 后真导入 `SCAILAdapter`，对模板 `discover_nodes` + 两段 `modify_workflow_for_segment` 断言全部通过。 |

### 8.2 完美模仿的关键修正
SCAIL-2 中「角色身份」与「动作连贯」是**两个不同输入**：身份来自参考图，连贯来自 `previous_frames`。原朴素写法会在后续段误把"前段末帧"当新身份，造成身份漂移。修正后：
- **角色参考图（`user_ref_path`）每段都注入** → 身份恒定，跨段不漂移；
- **`previous_frames` 仅后续段串联前段末帧** → 动作连贯；
- 驱动视频 `skip_first_frames = seg.start_frame` → 每段取对应动作区间；
- 验证脚本确认：段1 的 `ref_image` 仍为 `user_ref.png`（非前段末帧），`previous_frames=["yunjii_scail_prev_frame",0]`，驱动偏移=80，核心 `segment_index=2`。

### 8.3 待真模型验证（非代码问题）
- 本机下载 SCAIL-2 14B 权重 + SAM3，确认 `WanSCAILToVideo` 真实 widget 名（模板里的 `segment_index/frame_count/width/height/prompt/replace_mode/previous_frames` 为基于文档的推测，可能需微调）；
- 在真实 ComfyUI 跑一次端到端，确认 14B 推理链路与显存占用（建议 24G+）；
- 长视频：planner 负责镜头/风格分段，SCAIL 原生 Base/Extend 负责单镜头时长延展，stitcher 做段间交叉淡化，三层叠加即「长视频完美模仿」。

### 8.4 下一步
- 真模型验证通过后，把 SCAIL 路线作为「模仿模块」默认后端；
- 继续 P1（清理 `effects/mimic.py` 空实现等死代码 + 对齐技术文档）、P2（归档散落脚本/版本 JSON）、P3（落地 Phase 2 运镜/创意/增强效果模块）。
